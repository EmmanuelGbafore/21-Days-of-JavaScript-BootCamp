// Expression function to Calculate the area of a square
const areaOfSquare = function (side){
    return Math.pow(side,2);
}
console.log(areaOfSquare(5));        // Enter any number in the inner parenthesis