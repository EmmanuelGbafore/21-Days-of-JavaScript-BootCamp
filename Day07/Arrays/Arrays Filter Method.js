// Code to filter even numbers from a list of numbers
let numbers = [5, 6, 7, 8, 9, 10, 11, 12];
let evenNumbers = numbers.filter((num) => num % 2 === 0);
console.log(evenNumbers);  // Should print out all even numbers
