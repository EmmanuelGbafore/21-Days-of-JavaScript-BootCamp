// The push method adds an element at the end of the array
let vowels = ['a','e','i','o'];
console.log(vowels);      // Prints out the list before the addition of the element.
vowels.push('u');
console.log(vowels);    // Will print the list of vowels with u added to the end