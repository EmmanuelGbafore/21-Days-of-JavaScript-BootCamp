// The splice method removes elements and replace with other elements
let vowels = ['a','e','i','p','v'];
console.log(vowels);      // Prints out the list before the removal the elements.
vowels.splice(3,4, 'o','u');
console.log(vowels);    // Will print the list of vowels with p  removed from the end